<!DOCTYPE html>
<html>
<meta charset="UTF-8">
<title>Title</title>
<link rel="stylesheet" type="text/css" href="styles/style.css">
<body>
<div>
    <form method="post">
        <input type="text" name="name" placeholder="Введите название места">
        <select name="parent"><option>does not have a parent</option>{{#places}}<option>{{placeName}}</option>{{/places}}</select>
        <button type="submit">Add</button>
    </form>
</div>
<div>Список мест</div>
<table border="0">
    <thead>
        <td>Id</td>
        <td>Place</td>
        <td>Parent</td>
    </thead>

    {{#places}}
        <tbody>
            <td><a href="/place/{{placeId}}">{{placeId}}</a></td>
            <td><a href="/place/{{placeId}}">{{placeName}}</a></td>
            <td>
                {{#parent}}
                <a href="/place/{{parent}}"> {{parent}}</a>
                {{/parent}}
                {{^parent}}None{{/parent}}
            </td>
        </tbody>
    {{/places}}
</table>
<br/><br/>
<div>
    <a class="button" href="/">Back</a>
</div>
</body>
</html>